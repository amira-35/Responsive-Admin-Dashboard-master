import 'package:flutter/material.dart';

class Servers extends StatelessWidget {
  static const String id = "servers-screen";
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("servers"),
    );
  }
}
