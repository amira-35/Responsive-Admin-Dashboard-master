import 'package:flutter/material.dart';

class Licenses extends StatelessWidget {
  static const String id = "licenses-screen";
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("licenes"),
    );
  }
}
