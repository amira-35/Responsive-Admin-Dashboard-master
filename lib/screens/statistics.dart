import 'package:flutter/material.dart';

class Statisics extends StatelessWidget {
  static const String id = "statistics-screen";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
