import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  static const String id = "dashborad-screen";
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("dashborad screen "),
    );
  }
}
